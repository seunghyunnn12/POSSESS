# 0.10 이야기와 구역 전환

## 바뀐 경험

시작 화면에서 몸과 이름을 잃은 유령이 종을 멈추기 위해 내려간다는 목적을 제시한다. 기존 일곱 구역의 장 제목·짧은 이야기, 첫 출발, 보스 안내, 최종 결과로 연결한다. 연습을 건너뛴 경우에도 모순되지 않도록 문구를 맞췄다.

구역 이동에는 2D 삽화·목적지·경로·짧은 팁이 표시된다. 기존 1.2초 전환을 늘리거나 가짜 로딩 진행률을 넣지 않았다. 맵 생성은 기존 동기 방식이다. 빠른 전환이므로 긴 팁을 읽히는 용도는 아니다. 첫 출발은 준비 화면에서 기다리고 이후 일반 방은 자동 진입한다.

보스 도입·봉인 해제 문구와 종소리, 문 이동음을 연결했다. 같은 프레임의 일반 유물 알림이 봉인 해제 문구를 덮지 않는다. 보스방의 높은 종과 벽 장식은 충돌·이동·공격 판정을 바꾸지 않는다. 결과에는 이야기의 마무리와 실제 유령·증강 종류 수·완성 조합 수를 표시한다. authority/action/journey/campaign은 변경하지 않았다.

## 화면과 아트

- [시작](qa-output/v10-title.png)
- [구역 전환](qa-output/v10-passage.png)
- [첫 출발](qa-output/v10-departure.png)
- [결과](qa-output/v10-ending.png) — QA 결과 상태이며 실제 완주 성과가 아니다.

왼쪽 제목·이야기 → 오른쪽 종문 → 아래 경로·팁 순서로 읽는다. 기존 청록·황동·먹색 Theme와 OFL 한글 폰트를 유지했다. 글자는 모두 번역 키로 표시한다.

삽화는 내장 imagegen으로 생성한 1672×941 PNG이며 `assets/ui/illustrations/bell_gate.png`에 동봉했다. 실제 맵 스크린샷이 아니며 **모든 구역이 한 장을 공유한다.** 다른 아트 슬롯 13개는 비어 있다. [슬롯 규격](docs/ui/ART_SLOTS.md), [생성 기록](assets/ui/illustrations/README.md).

## 검사

기존 406개 + 이야기 14개 = **420개 통과**. 이야기 검사는 도입·아트·실제 방 교체 앞뒤의 목적지 일치·생존 시간 보존·일시정지 가림 방지·기존 전환 시간·출발 입력·보스 알림 충돌·변경한 키로 팁 표시·엔딩·보조 버튼 문구를 포함한다.

최종 로그는 `v10-{smoke,journey,campaign,action,ghosts,feedback,bindings_records,ui,evolution-render,settings-render,story}.log`이며 모두 실패·스크립트 오류 0이다. 실제 창 UI 검사는 한 번 일시정지가 뒤집힌 상태로 실패했고 단독 재실행은 통과했다. 창 포커스 영향으로 의심하지만 원인을 확정하지는 않았다. 캡처는 화면 전환 후 세 프레임을 기다려 텍스트 배치가 완료된 화면을 남겼다.

배포물은 `builds/windows-v10/POSSESS.exe`와 `builds/POSSESS-0.10-Windows.zip`. EXE의 연습 시작·렌더링·자동 종료 코드 0을 확인했고 빌드 로그에서 삽화의 PCK 포함을 확인했다. 외부 QA 스크립트로 릴리스 전환 화면을 캡처하는 추가 시도는 응답하지 않아 검사 프로세스만 종료했다. 이 시도는 성공 검사에 포함하지 않는다. 전환 화면 자동 검증은 Godot 콘솔 실행기에서 수행했다.

## 성능과 남은 일

RX 580 2048SP, 1280×720, Compatibility, 기존 유령 스트레스 장면: 평균 **16.68ms / 59.9fps**, p95 **17.40ms**, p99 **18.17ms**, 최대 투사체 11개. [측정 결과](qa-output/v10-performance.txt). 이전과 같은 평균이지만 단일 측정으로 최적화 달성을 주장하지 않는다. p99 < 16.7ms 목표는 미달이다.

구역별 전용 삽화·3D 환경, 보스 고유 등장 애니메이션, 음악·전문 사운드, 초상화·증강 아이콘, 외부 플레이테스트·밸런스, 영어 번역은 남아 있다. 이번 작업은 경험의 연결을 보완한 단계이며 상용 출시 완료가 아니다.
