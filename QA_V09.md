# 0.9 조작 키 · 지난 원정 · Windows 배포

## 플레이어 기능

- Esc → 조작에서 이동 4방향, 상호작용, 재장전, 몸 이탈, 회피, 점프를 재지정한다. 버튼을 누른 뒤 새 키를 누르며 Esc는 입력 대기만 취소한다. 중복 키와 메뉴 필수 키는 거부한다. 키 기본값 복원을 제공한다.
- 키는 기존 기기 설정 파일에 저장된다. 실제 이동·상호작용 입력과 행동 요청에 적용되고, 문·보급 등 HUD 안내도 바뀐 키를 표시한다. 마우스 버튼과 메뉴 단축키는 고정이다. 게임 중 재장전 키를 바꿔도 증강 화면의 R과 결과 화면의 R은 그대로다.
- 타이틀의 지난 원정 또는 Esc → 지난 원정에서 최근 결과를 본다. 실제 원정의 완료·사망 이벤트마다 한 번 저장한다. 연습방과 방 정리, 진행 중 종료는 완료 기록에 포함하지 않는다.
- `user://run_records.cfg`에 최근 20판의 유령·결과·도달 구역·처치·빙의·전투 시간·시드를 저장한다. 화면에는 최신 8판을 표시한다. 임시 파일 교체와 이전 복구본을 사용하며 형식이 잘못된 기록은 건너뛴다. 진행 중인 원정 이어하기는 아니다.

전투 규칙과 authority/action 판정 코드는 수정하지 않았다.

## 화면

- [키 재지정](qa-output/v09-bindings.png)
- [지난 원정](qa-output/v09-records.png) — 표시 공간 확인용 QA 기록이며 실제 플레이 성과가 아니다.
- [기기 설정](qa-output/v09-settings.png)

기존 Theme·폰트와 탭 위계를 유지했다. 조작 화면은 행동 이름과 키 버튼을 두 열로 배치하고, 기록 화면은 시간순 목록을 사용한다. 새로운 아트 슬롯은 없다.

## 검증

| 검사 | 통과 |
|---|---:|
| smoke | 47 |
| evolution 실제 창 입력 | 77 |
| journey | 28 |
| campaign | 70 |
| action | 50 |
| ghosts | 51 |
| UI | 41 |
| feedback | 8 |
| settings 실제 창 입력 | 17 |
| 키 재지정·기록 | 17 |
| **합계** | **406** |

새 검사는 키 충돌·예약 키·취소·실제 명령 전달·이전 키 비활성화·파일 재로딩·안내 동기화·실제 사망 이벤트·중복 방지·기록 복구·저장 및 표시 상한을 포함한다. 정상 로그는 `v09-{smoke,evolution-render,journey,campaign,action,ghosts,ui,feedback,settings-render,bindings-records-render}.log`다. QA는 실제 사용자 설정·기록 파일을 수정하지 않는다.

## Windows 빌드

공식 4.7.1 템플릿의 SHA512를 검증했고, 준비 스크립트와 빌드 스크립트를 실행했다. [재현 절차](BUILD.md).

최종 배포물: `builds/POSSESS-0.9-Windows.zip`. ZIP을 별도 폴더에 풀어 **편집기 없이 EXE + PCK로 연습방을 실행하고 종료 코드 0**을 확인했다. 시작 로그에 오류가 없다. 이 검사는 시작·연습방의 렌더링·자동 종료이며 별도 PC에서의 완주 검증은 아니다. [실행 로그](qa-output/v09-packed-play.log).

ZIP SHA256: `E8694E490F84575FE5BFA9E52569DBA363C593797E757E51DD0BA5D71B874AFE`.

ZIP은 로컬 산출물이며 Git에 대용량 바이너리를 넣지 않는다. 소스·준비 및 빌드 스크립트·프리셋·QA 기록을 커밋한다. 배포물에는 안내와 엔진·글꼴·모델 라이선스를 동봉한다. 저장 호환성을 위해 기존 사용자 폴더 이름을 유지한다.

## 성능과 남은 일

기존 유령 스트레스 장면, RX 580 2048SP / 1280×720 / Compatibility: 평균 **16.68ms / 59.9fps**, p95 **17.16ms**, p99 **19.04ms**, 최대 투사체 10개. [측정 결과](qa-output/v09-performance.txt). 에디터 콘솔 실행기로 측정했으며 릴리스 빌드의 성능 측정으로 주장하지 않는다. 목표 p99 < 16.7ms는 여전히 미달이다.

남은 범위는 프레임 안정화, 별도 PC·장시간 플레이테스트, 재미·밸런스 조정, 영어 번역과 언어 선택, 최종 아트·사운드, Steam 배포 준비다. 협동은 별도 단계다. 이번 빌드는 플레이테스트용이며 상용 출시 완료가 아니다.
