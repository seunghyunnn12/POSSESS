# Astra 과제서 — M1: UI 시각 패스 (실제 게임처럼 보이게)

UI의 시각 디자인은 Astra가 직접 잡는다. `docs/ui/`의 목업·스타일 가이드는 참고 자료일 뿐 스펙이 아니며 따르지 않아도 된다.
이 과제 밖의 새 시스템·콘텐츠·규칙 추가 금지. 판정 계층(authority/action)은 손대지 않는다.

## 디자인 목표
- "실제 상용 게임처럼" 보일 것. 텍스트를 정돈하는 수준이 아니라 프레임·아이콘 자리·위계·여백이 있는 진짜 게임 UI.
- 톤: 캐주얼하되 유치하지 않게, 어둡고 서늘한 지하. 참고 게임: [유저가 지정].
- 시스템 폰트(Malgun Gothic) 사용 금지. 한/영 모두 지원하는 OFL 등 상용 무료 폰트를 골라 TTF와 라이선스를 동봉.
- AI가 만든 듯한 밋밋한 카드·균일한 패널 나열을 피할 것. 화면마다 시선이 먼저 가는 한 지점을 정하고 나머지는 종속시킨다.

## 구현 요구
1. Theme 리소스 하나(`theme/possess.tres`)로 색·폰트·패널 스타일 통합. 모든 UI는 이 Theme만 쓴다.
2. 화면 4종을 Control 씬으로 재구축: HUD, 증강 선택, 타이틀, 일시정지·정보창. `hud.gd`/`action_hud.gd`의 `draw_string` 즉시 모드는 제거. UI는 authority 신호만 관찰.
3. 아이콘·초상화·카드 일러스트·키아트 자리는 `TextureRect` placeholder로 비워 둔다(이미지 파일만 넣으면 되도록). 자리의 크기·비율은 UI 설계 시 확정해 `docs/ui/ART_SLOTS.md`에 목록으로 남긴다(2D 아트 제작 브리프의 근거).
4. 정보 정리: 상시 노출 텍스트는 HUD 4곳(스테이지·남은 적·몸/수명·무기/탄창)+영혼 바만. 같은 안내가 두 번 뜨지 않는다(무기 팁 중복, 상시 "위장" 문구 제거). 상태 변화 안내는 1.5초 토스트 1줄, 한 번에 하나.
5. 같이 처리(배선): 화면 문자열 전부 `tr("KEY")` + `locale/ko.csv`(영어 열 비움, 별도 담당). 모델 경로·스케일·애니 클립 이름을 `scripts/visuals.gd` 테이블로 이동, 규약은 `ASSET_CONVENTIONS.md`.

## 수용 기준
1. 4개 화면 스크린샷을 `qa-output/v06-*.png`로 제출. 디자인 의도(시선 순서·색 역할·폰트 선택 이유)를 QA_V06.md에 5줄 이내로.
2. `draw_string` 직접 호출 없음. 시스템 폰트 없음.
3. 기존 검사 261개 통과 + 언어 전환·테이블 로딩 검사. 성능 회귀 없음.
4. 상시 텍스트가 HUD 4곳+영혼 바 외에 없다.
5. 작업 단위별 커밋, `git push origin main`.

## 이후 예정 (지금 하지 말 것)
M2 영구 해금·세이브·유령 3종·설정 메뉴 / M3 60fps·Windows 빌드·Steamworks / 아트 패스(AI 3D 모델·애니·환경·VFX·사운드) / M4 협동.
