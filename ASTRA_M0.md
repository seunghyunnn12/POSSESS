# Astra 과제서 — M0: 빙의 코어 루프 재미 검증

## 환경
- Godot 4.7.1 (GDScript), 프로젝트 루트: `C:\Users\shtmd\Desktop\possess-game`
- 이 폴더에 새 Godot 프로젝트를 생성할 것 (GDD.md 참고). 결과물은 실행만으로 확인 가능해야 함.

## 만들 것 — 방 하나짜리 FPS 프로토타입

### 1. 유령 (플레이어 본체)
- 1인칭. WASD + 마우스, 이동 빠르고 관성 가벼움. 점프 낮게, 부양감(중력 0.6배).
- 좌클릭: 기본 공격(직선 저주탄, 낮은 데미지, 발사감 있게 — 발사음/트레이서/히트마커).
- **소멸 게이지**: 유령 상태에서 초당 감소(약 20초 분량). 0이 되면 사망 → R로 리스타트.
- 우클릭: 조준한 적에게 빙의 시도(사거리 6m). 성공 → 그 몸으로 전환. 실패 → 0.5초 넉백.

### 2. 빙의 확률 (포켓몬 문법)
- 적 머리 위에 항상 확률 표시: `기본 15% + (잃은 체력 비율 × 70%)` → 빈사 적은 85%.
- 성공/실패 연출 필수: 성공 = 화면이 빨려들어가는 0.3초 전환, 실패 = 튕겨나오는 넉백 + 효과음.

### 3. 몸 (빙의 상태)
- **병사**: 라이플(연사, 반동, 재장전). 이동 보통.
- **브루트**: 근접 휘두르기(넓은 판정, 화면 흔들림), 느리지만 단단함.
- **부패 게이지**: 빙의 순간부터 감소(병사 25초, 브루트 35초). 피격 시 추가 감소.
  화면 가장자리 변색 + 팔 모델 색 변화로 잔량 표현 (게이지 바 + 시각 연출 둘 다).
- 부패 만료: 몸 폭발(주변 데미지) → 유령 복귀, 소멸 게이지 리셋.
- E키: 자발적 이탈(폭발 없음).

### 4. 적 AI (최소)
- 병사: 플레이어 조준 사격, 엄폐 없이 거리 유지만.
- 브루트: 돌진 근접.
- 방에 병사 3 + 브루트 2 배치, 전멸 시 "CLEAR" 표시 + R로 재시작.

### 5. 아키텍처 제약 (중요 — 멀티 대비)
- 플레이어 입력 → 상태 변경은 전부 서버 권위 구조로: 로직을 `authority` 노드에서 처리하고 시각 표현과 분리할 것. Godot high-level multiplayer(MultiplayerSynchronizer)를 나중에 붙일 수 있는 구조.
- 유령/몸 전환은 상태 머신으로: `Soul / Possessing / Body / Ejecting` 4상태.

### 6. 아트 (임시)
- 프리미티브 + 단색 머티리얼 OK. 단, 유령 시야는 모노톤+비네트, 몸 시야는 컬러 — 상태 전환이 화면 톤으로 즉시 구분될 것.
- 어두운 방 + 발광 포인트 조명.

## 수용 기준 (이걸로 합격/불합격 판정)
1. 튜토리얼 텍스트 없이 첫 플레이에서 "약화→빙의→부패→갈아타기"를 스스로 발견할 수 있다.
2. "유령→약화→빙의→몸 싸움→탈출"의 20초 루프가 3회 이상 반복하고 싶은 손맛이 난다.
3. 빙의 성공 순간이 이 게임 최고의 쾌감이다 (전환 연출 + 사운드).
4. 60fps 유지.

## 금지사항
- 증강/레벨업/보스/유령 종류 추가 금지 — M0은 오직 코어 루프.
- 기존 파일 덮어쓰기 금지, 이 폴더 밖 수정 금지.
