# 0.8 설정과 전투 피드백 정비

## 이번 작업

- 타이틀의 설정 버튼 또는 Esc → 설정에서 마우스 감도, 효과음 크기, 전체 화면, 창 크기, 상하 반전, 수직 동기화를 변경한다. 기본값 복원을 제공한다.
- 설정은 즉시 적용되고 `user://settings.cfg`에 저장된다. 임시 파일 교체와 이전 설정 복구본을 사용한다. 잘못된 타입·범위·NaN은 거르고, 설정 섹션이 없으면 복구본을 읽는다. 저장 실패는 설정 화면에 표시한다.
- 창 크기는 1280×720, 1600×900, 1920×1080 중 선택하며, 사용 가능한 화면보다 크면 비율을 유지해 줄인다. 전체 화면에서는 창 크기 선택을 비활성화한다.
- 화염·번개·처치 확인 소리를 구분한다. 산탄과 연쇄 공격의 일반 타격음은 45ms 이내 중복 재생을 제한한다. 합성 효과음의 구분을 개선한 것으로 최종 사운드 에셋을 대체하지 않는다.
- 연속 피격은 이전 회전 트윈을 교체하고, 사망 시 피격 회전을 취소한다. 방 이동 시 잔여 빔·폭발·경험치 표시·투사체 표시와 일시정지된 트윈을 정리한다.

전투 판정 계층은 변경하지 않았다. 설정은 기존 Theme와 번역 CSV를 사용한다. 테스트 실행은 실제 사용자 설정 파일을 읽거나 쓰지 않는다. M 키 음소거는 기존처럼 현재 실행에만 적용되는 별도 토글이다.

## 화면

[설정 화면](qa-output/v08-settings.png)

설정 제목 → 감도·음량 → 화면·입력 옵션 → 복원 순서로 읽는다. 민트는 조절값·활성 상태, 금색은 화면 구분선, 회색은 보조 설명에 사용한다. 기존 OFL Noto Sans KR과 공유 Theme를 유지했다.

## 검사

| 검사 | 통과 |
|---|---:|
| smoke | 47 |
| evolution 실제 창 입력 포함 | 77 |
| journey | 28 |
| campaign | 70 |
| action | 50 |
| ghosts | 51 |
| UI | 41 |
| 전투 연출 | 8 |
| 설정 실제 창 입력·화면 전환 포함 | 17 |
| **합계** | **389** |

최종 제출 로그는 `v08-{smoke,evolution-render,journey,campaign,action,ghosts,ui,feedback-render,settings-render}.log`이며 모두 실패 및 스크립트 오류 0이다. 설정은 파일 재로딩·반복 교체·스키마 손상 복구·쓰기 실패, 실제 마우스 감도와 반전, 메뉴 중 조준 차단, 전체 화면과 창 모드 왕복, 수직 동기화 적용을 검사했다. 기존 아트 슬롯 14개는 그대로다. UI 검사는 OptionButton 내부 엔진 노드를 아트 슬롯에서 제외한다.

## 성능과 남은 범위

RX 580 2048SP, 1280×720, Compatibility, 기존 유령 투사체 스트레스 장면: 평균 16.68ms / 59.9fps, p95 17.39ms, p99 19.22ms, 최대 투사체 10개. [측정 결과](qa-output/v08-performance.txt). 이전 v0.7의 p99 18.64ms보다 이번 단일 측정이 높으므로 무회귀 또는 안정적인 60fps 달성을 주장하지 않는다. 로드맵의 p99 < 16.7ms 기준은 미달이다.

키 재지정, 영어 번역·언어 선택, 원정 기록 저장, Windows 배포 빌드, 성능 목표 달성, 최종 아트·사운드와 외부 플레이테스트는 아직 남아 있다. 시작 유령 해금 저장과 기기 설정 저장이 있으며 진행 중 원정 이어하기는 없다.
