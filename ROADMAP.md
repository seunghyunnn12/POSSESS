# POSSESS — 완성 로드맵 (2026-09-09)

## 구현 현황 — 2026-09-11 / 0.10

- 도입 목적과 일곱 구역의 짧은 이야기, 2D 구역 전환 삽화·팁·경로 표시, 보스 도입·봉인 해제 안내와 종소리, 엔딩 및 실제 빌드 요약을 연결했다.
- 기존 이동 시간·전투 판정을 유지했다. 보스방 종과 벽 장식은 비충돌 시각 요소다. 삽화는 현재 한 장을 공유한다.
- 검사 420개 통과. 평균 59.9fps, p99 18.17ms로 프레임 안정화 목표는 미달이다.
- 다음은 구역별 실제 환경·전용 아트, 고유 보스 연출, 음악·사운드와 보상 아트, 외부 플레이테스트다. 상용 출시 완료로 보지 않는다.

## 구현 현황 — 2026-09-10 / 0.9

- 추가 완료: 키보드 조작 재지정·기기 설정과 함께 저장·문 안내 동기화, 완료/사망 원정 최근 20판 저장·최신 8판 표시.
- Windows x86_64 릴리스 EXE/PCK 및 ZIP 생성, 별도 폴더에 압축 해제 후 편집기 없는 시작·연습방·종료 확인. 재빌드 스크립트와 공식 템플릿 체크섬 검증 포함.
- 회귀 및 신규 검사 406개 통과. 성능 평균 59.9fps, p99 19.04ms로 프레임 안정화 목표는 미달.
- 다음 검증: 다른 PC에서 빌드 완주·장시간 안정성·외부 플레이테스트와 밸런스. 영어 번역·언어 선택, 최종 아트·사운드, Steam 준비는 남아 있다.
- M2의 키 변경과 기록 저장은 구현했지만 영어까지 포함한 설정 요구는 미완료이며, M3 전체 완료도 아니다.

## 구현 현황 — 2026-09-10 / 0.8

- 완료: Control UI·공유 Theme·한글 무료 폰트·번역 테이블·모델 교체 테이블, 시작 유령 4종과 조건 해금 저장, 기본 기기 설정 저장.
- 설정 완료 범위: 감도·음량·전체 화면·창 크기·상하 반전·수직 동기화·기본값 복원.
- 다음 구현: 키 재지정과 조작 안내 동기화, 원정 기록 저장, 배포 빌드 점검. 영어 번역은 기존 담당 범위로 남긴다.
- 성능은 평균 59.9fps지만 p99 19.22ms로 목표 미달이다. M3 완료로 표시하지 않는다.
- 최종 아트·사운드 교체와 외부 플레이테스트, 재미·밸런스 검증은 별도로 남아 있다. 현재 검사 389개 통과는 상용 게임 완성을 뜻하지 않는다.

아래 정의와 역할 분담은 기존 기준을 유지한다.

원칙: **게임을 먼저 완성한다. 아트(모델·애니메이션·환경·VFX)는 나중에 덧씌운다.**
단, "덧씌울 수 있는 구조"를 지금 만들어 두는 것까지가 완성의 일부다.

## 완성의 정의 (Definition of Done — 아트 제외)

1. 첫 실행부터 엔딩까지 설명 없이 진행되는 한 판 (연습 → 7구역 → 최종보스 → 결과)
2. 죽으면 처음부터, 그러나 **영구 해금**(유령 종류·기록)이 남는 메타 진행 + 저장
3. 유령(플레이어 캐릭터) 3종 이상, 빌리는 몸 6종, 보스 2종, 방 구조 3종 이상
4. 설정 메뉴: 해상도·창모드·감도·볼륨·키 리바인딩·언어(한/영)
5. 모든 문자열이 번역 테이블을 거침 (영어 로컬라이즈 가능)
6. UI가 Control/Theme 기반이며 아이콘·프레임 자리가 비어 있는 상태로 준비됨
7. 캐릭터·무기·환경 모델이 **데이터 테이블 한 곳**에서 교체됨 (경로·스케일·소켓·애니 클립 이름)
8. 1280×720 GL Compatibility에서 60fps 안정 (p99 < 16.7ms), Windows 내보내기 빌드가 돌아감
9. 핵심 외 시스템(위장·감시자·개체값·의뢰·유물)은 플래그로 켜고 끌 수 있음 — 재미 검증 결과에 따라 결정

## 마일스톤

| 단계 | 내용 | 담당 |
|---|---|---|
| **M1 완성 뼈대** | 에셋 교체 규약 + 로컬라이즈 인프라 + Control 기반 UI 재구축 + 시스템 플래그 | Astra (과제서 `ASTRA_M1.md`) |
| **M2 메타·저장·설정** | 영구 해금, 세이브, 유령 3종, 설정 메뉴, 키 리바인딩 | Astra |
| **M3 빌드·성능·출시 준비** | 60fps 안정화, Windows 내보내기, 크래시 로그, Steamworks 연동 | Astra + Claude(Steam 페이지·빌드 검수) |
| **아트 패스** | AI 3D 생성 모델·애니메이션·환경·VFX·사운드를 규약대로 교체 | Claude(조달·생성·통합) + Astra(VFX/애니 트리 코드) |
| **M4 협동** | 2~4인 온라인 | Astra |

## 역할 분담

**유저 (유일한 플레이어)**: 재미·난이도 판정. 각 빌드마다 "재밌었나 / 짜증났나 / 이해 안 된 것" 세 줄.

**Astra (구현)**: 위 마일스톤의 코드 작업 전부. 과제서 단위로 진행, 과제서 밖 시스템 추가 금지.

**Claude (프로듀서·검수·아트 파이프라인)**:
- 마일스톤별 과제서 작성 + 결과물 검수 (코드 + 스크린샷)
- 에셋 교체 규약(`ASSET_CONVENTIONS.md`) 정의 — AI 3D 모델이 나중에 그대로 꽂히도록
- 영어 로컬라이즈 (번역 테이블 전량 직접 작성)
- Steam 준비: 스토어 문구·캡슐 브리프·가격·태그·Steamworks 체크리스트
- 에셋 조달·생성(MCP 연결 후)·통합, 라이선스 관리
- 밸런스 수치 검토, 경쟁작 모니터링, git/배포 관리
